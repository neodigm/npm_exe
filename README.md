# Streamable LangChain demo

Run the local executable server with:

```bash
npm start
```

The demo serves a streaming endpoint at `/rai/stream_events` and a simple UI at `/`.

Set `ANTHROPIC_API_KEY` in your environment before using the real LLM stream.
